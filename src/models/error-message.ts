import 'reflect-metadata';

export enum ErrorMessage {
    UNKNOWN_ERROR = -1,
    INVALID_COUNT = 1,
    INVALID_COUNT_RANGE = 2,
    INVALID_DATE = 3,
    INVALID_DATE_RANGE= 4,
    RECORDS_NOT_FOUND = 5
}
